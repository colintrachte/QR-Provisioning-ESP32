#esp-idf components
managed_components/
dependencies.lock

# PlatformIO build output
*.gz
# PlatformIO build directory
.pio/
.pioenvs/
.piolibdeps/

# Arduino build artifacts
build/
bin/
dist/
*.hex
*.bin
*.elf
*.map
# compile_commands.json is generated by "pio run" and used by clangd.
# Excluding it means anyone who clones must build once before
# C IntelliSense fully works — document this in your README.
# Alternatively, commit it for instant clangd support on clone:
# (uncomment the next line to commit it)
# !.pio/build/tractor_esp32/compile_commands.json

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor state (personal, not team config)
.vscode/*
!.vscode/extensions.json
!.vscode/arduino.json
.vscode/launch.json
.vscode/c_cpp_properties.json

# JetBrains / CLion
.idea/
cmake-build-*/

# Editor backups
*.swp
*.swo
*~

# Sensitive information
*.pem
*.key
.env

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# macOS
.DS_Store
.AppleDouble
.LSOverride
